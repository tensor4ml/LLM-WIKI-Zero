# Graphify Validate Mode

Validate:

- output files exist,
- graph data is nonempty,
- expected Wiki pages are represented,
- links are not obviously malformed,
- the graph is labeled structural when semantic extraction was unavailable.

Do not rewrite Wiki pages during validation.
